// node_modules/@hyperledger/aries-oca/build/utils/color/hashCode.js
var hashCode = (s) => {
  return s.split("").reduce((hash, char) => char.charCodeAt(0) + ((hash << 5) - hash), 0);
};
var hashCode_default = hashCode;

// node_modules/@hyperledger/aries-oca/build/utils/color/mulberry32.js
var mulberry32 = (seed) => {
  let t = seed += 1831565813;
  t = Math.imul(t ^ t >>> 15, t | 1);
  t ^= t + Math.imul(t ^ t >>> 7, t | 61);
  return ((t ^ t >>> 14) >>> 0) / 4294967296;
};
var mulberry32_default = mulberry32;

// node_modules/@hyperledger/aries-oca/build/utils/color/hashToRGBA.js
var hashToRGBA = (hash) => {
  let color = "#";
  const colorRangeUpperBound = 256;
  for (let i = 0; i < 3; i++) {
    color += ("0" + Math.floor(mulberry32_default(hash + i) * colorRangeUpperBound / 2).toString(16)).slice(-2);
  }
  return color;
};
var hashToRGBA_default = hashToRGBA;

// node_modules/@hyperledger/aries-oca/build/utils/color/generateColor.js
var generateColor = (seed) => {
  return hashToRGBA_default(hashCode_default(seed));
};
var generateColor_default = generateColor;

// node_modules/@hyperledger/aries-oca/build/utils/color/luminanceForHexColor.js
var luminanceForHexColor = (hex) => {
  if (!/^#([A-Fa-f0-9]{6})$/.test(hex)) {
    return;
  }
  const hexAsNumber = Number(`0x${hex.slice(1)}`);
  const [r, g, b] = [hexAsNumber >> 16 & 255, hexAsNumber >> 8 & 255, hexAsNumber & 255];
  const y = 0.2126 * r + 0.7152 * g + 0.0722 * b;
  return Math.round(y);
};
var luminanceForHexColor_default = luminanceForHexColor;

// node_modules/@hyperledger/aries-oca/build/utils/color/textColorForBackground.js
var textColorForBackground = (background) => {
  const spectrum = {
    black: "#000000",
    darkGrey: "#313132",
    mediumGrey: "#606060",
    lightGrey: "#D3D3D3",
    veryLightGrey: "#F2F2F2",
    white: "#FFFFFF"
  };
  const midpoint = 255 / 2;
  if ((luminanceForHexColor_default(background ?? "") ?? 0) >= midpoint) {
    return spectrum.darkGrey;
  }
  return spectrum.white;
};

export {
  generateColor_default,
  textColorForBackground
};
//# sourceMappingURL=chunk-DVF5LCIG.js.map
